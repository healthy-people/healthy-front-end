import React from 'react';

function boozeChallenge() {
    return <div>
        <h1>Challenge created! Whoever doesn't drink for *length chosen* wins!</h1>
        <h2>If nobody lasts *length chosen*, whoever is the last to drink wins!</h2>
    </div>
}

function cigChallenge() {
    return <div>
        <h1>Challenge created! Whoever doesn't smoke for *length chosen* wins!</h1>
        <h2>If nobody lasts *length chosen*, whoever is the last to smoke wins!</h2>
    </div>
}

export default boozeChallenge;
export default cigChallenge;